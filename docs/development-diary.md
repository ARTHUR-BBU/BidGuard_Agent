# BidGuard Agent 开发日记

这不是技术提交记录的翻译，而是写给项目所有者的“第二本账”：这轮做了什么、凭什么相信、还剩什么风险、需要谁拍板。

## 第一章 · 先立规矩，再请 AI 上桌（09-18，PR 号待补 · 治理总纲补记）

### 这次做了什么（大白话）

- **给项目立了宪法**：像先写清法庭里谁能作证、谁能裁决。招标原文定义要求，企业现有材料支撑事实，AI 负责理解和质疑，程序负责校验，最终承诺仍由人作出。
- **给未来模型装上权限围栏**：AI 可以提候选问题，却不能把猜测写成公司事实，也不能偷偷替人提交投标文件。
- **把开发分阶段设门禁**：真实业务文本进入模型前，必须先通过证据、覆盖范围、预算、调用台账和人工确认等检查。

### 给您的交代

治理总纲提交为 `a9cc6a8`，包括项目宪法、LLM 权限与分阶段开发规范，以及强制开发入口。该里程碑经独立复核后放行。它建立的是团队必须遵守的规则，不代表真实模型已经上线，也不代表审核效果已经得到业务验证。

### 需要您拍板

目前没有需要立即拍板的事项。未来进入真实模型调用前，会单独提交模型供应商、预算上限和真实文本测试范围供您确认。

### 待办登记

见文末统一待办清单。

## 第六章 · 要求先钉在原文上，再进入后续审核（09-21，Task 9）

### 这次做了什么（大白话）

- **做了一个“拆单员”**：把招标摘录里的复合要求拆成后续可以逐条核对的原子要求，但不替人判断企业是否合规。
- **给每条要求装上“门牌号”**：模型必须给出当前招标版本、页码或章节和最小原文引用；引用不在授权片段里，就不入库。
- **把两种指纹分开**：一枚指纹表示“要求内容是什么”，另一枚表示“它引用了哪个版本、哪一页/章节”，避免同一句话在不同位置被错误合并。
- **保留历史**：换了新的招标版本，旧要求只变成 inactive，不删除，方便追溯“当时依据了哪一版文件”。
- **把模型调用记账**：真正执行 Runner 时必须写入安全台账；正文摘录还必须与数据库授权 chunk 完全对应，防止把外部提示注入混进模型输入。

### 先说难堪的

这一步仍然没有把真实招标文件文本交给模型。现在通过的是“程序边界 + mock 模型响应”的合约测试，不是模型业务质量测试，也不是完整招标包审核。

### 给您的交代

- Task 9 专项测试 **17 项通过**；全量测试 **261 项通过**；Ruff 与 Mypy 通过。
- 真实数据库只做了临时副本迁移演练，新增 `citation_fingerprint` 字段能够升级；真实 `backend/bidguard.db` 哈希保持不变。
- `Constitution impact: Yes` 已单独记录在 `docs/governance/task-9-constitution-impact.md`。

### 当前边界

Task 9 的 Step 1-5 已完成实现和测试，Step 6 的真实单文件提取 smoke 仍未执行；必须等独立复审通过并再次确认凭据、范围和结果留痕后，才可以做那一次有界 live 评测。不能把当前结果宣传成完整招标包覆盖或真实审核质量验收。

## 第七章 · 给 Agent 一套有门禁的证据工具箱（Task 10）

### 这次做了什么（大白话）

- **允许它看小块证据**：Agent 可以读取授权版本的指定页，也可以搜索当前项目中已经授权的投标响应和企业资料，但拿不到整份文档。
- **允许它提交候选意见**：Agent 可以提交 Assessment 候选、请求人工确认、创建行动项；这些都是“待程序和人工继续处理的草稿”，不是最终裁定。
- **给“已满足”装上刹车**：如果候选 Assessment 写成 `MATCHED` 却没有 Evidence，程序直接拒绝；旧版本、跨项目、引用不存在的 quote 也不能保存。
- **正式状态由程序计算**：模型不能自己写 `satisfied`。服务端根据 Requirement、EvidenceState、Severity 和人工确认规则计算展示状态。

### 先说难堪的

Task 10 仍然没有把完整审核流程交给模型。当前只是把工具箱造好并装上门锁；Review Agent 编排、后台任务、人工决定和增量复核还没有实现。

### 给您的交代

- Task 10 专项测试 **6 项通过**；全量测试 **267 项通过**；Ruff 与 Mypy 通过。
- `Constitution impact: Yes` 已单独记录在 `docs/governance/task-10-constitution-impact.md`。
- 这一阶段没有执行真实业务模型调用，也没有修改真实 `backend/bidguard.db`。

## 第三章 · 搜索可以帮忙找证据，但不能替证据盖章（09-19，PR 号待补 · Task 7 模型无关检索收官）

### 这次做了什么（大白话）

- **先把“找得到”做扎实**：系统按文字重合度寻找候选片段，像档案员先把相关页码放到桌上；它不会因为搜到了几个词，就宣布企业已经符合要求。
- **检索范围有围墙**：每次搜索必须带项目编号，服务端逐个核对版本是否属于这个项目。跨项目、过期或非法版本会整次拒绝，不会返回一部分“看似有用”的结果。
- **覆盖不完整就亮黄灯**：解析状态不是唯一依据。缺页、OCR、异常 Coverage、部分解析都会保留候选，但明确标记不完整；后续 Agent 不能把它当作全量证据。

### 先说难堪的

第一版检索通过了自动测试，却被独立验收连续拦下：先是发现版本 ID 只靠调用方自觉，可能跨项目取证；接着发现异常 Coverage JSON 可能被误判为完整。我们连续补了权限绑定和 fail-closed 校验，直到独立复审达到 Critical 0、Important 0、Minor 0 才收口。

### 给您的交代

- 最终提交链以 `81ef8a9` 开始，经 `dd9a820`、`dfabce3` 修复，最终为 `763f770`。
- 本地专项测试 **28 项通过**；本地全套测试 **220 项通过**；Ruff、Mypy 和差异检查通过。
- 独立最终验收：**Critical 0、Important 0、Minor 0，Ready = Yes**。
- 本任务明确 `Constitution impact: Yes`：新增了服务端项目授权绑定与 Coverage 防御门禁，但仍不创建 EvidenceLink、Assessment 或正式状态。
- 没有调用 OpenAI 或其他模型。当前能力是“有范围、有地址、有完整性标记的确定性候选检索”，不是证据相关性自动裁定。

### 需要您拍板

目前没有立即需要拍板的事项。进入 Task 8/8A 前，仍要先完成模型边界、调用账本、预算和人工确认门禁；不会因为检索已经可用就直接把整库材料交给模型。

### 待办登记

见文末统一待办清单。

## 第二章 · 文件会说话，也会承认自己没看见（09-18，PR 号待补 · Task 6 文档解析收官）

### 先说难堪的

第一版虽然测试全绿，独立验收仍两次拦门：先发现 PDF 附件、Word 页眉页脚、异常中断等内容可能被静默漏掉；修完后，又发现两个同名章节会被合并。原实现代理中途还遇到使用额度中断。代码没有丢失，但这说明“实现者自证通过”不能代替独立验收。现在这条规矩已落实为：实现、攻击式复审、修复、再次复审，缺一不可。

### 这次做了什么（大白话）

- **每段文字都带地址**：PDF 记真实页码，Word 记标题路径和物理章节序号。两个都叫“资格要求”的章节，也像同名住户拥有不同门牌，不会再混成一处。
- **看不见就主动举手**：扫描页、附件、图片、复杂表格、页眉页脚等当前不能完整提取的内容，会进入结构化“覆盖范围”记录，并把结果标成不完整；系统不能把“我没看到”说成“文件里没有”。
- **解析任务可以安全接班**：任务意外中断后，超过 15 分钟的旧任务可由新尝试接管；较旧结果不能覆盖较新结果，失败重试也不会毁掉上一次成功快照。
- **旧账本没有被拿来试刀**：数据库升级使用增量方式，测试在隔离环境完成，没有删除或重建真实本地数据库。

### 给您的交代

- 最终代码提交链以 `7812843` 开始，经 `63ed1fa`、`d79b1b1`、`7c327b3` 修复，最后由 `a01ff32` 关闭数据约束缺口。
- 本地专项测试 **97 项通过**；本地全套测试 **192 项通过**；代码风格检查和 38 个源文件的类型检查通过。
- 独立最终验收：**Critical 0、Important 0、Minor 0，Ready = Yes**。
- 真实 `backend/bidguard.db` 复核为 **151552 bytes**，SHA-256 为 `B43DDC1DF8FFE938F838EABDDCC5A214B9BCC762EBB2E5A2C2B81E14A338B9CB`，修改时间仍为 `2026-09-17 01:40:54 UTC`。
- 尚未调用 OpenAI 或其他模型，也没有读取 API Key。本章证明的是文档解析和证据定位地基，不是 AI 已经会审标书。

遗留边界：当前不执行 OCR，不还原 PDF/Word 表格几何结构，不提取 PDF 附件，也不解析 Word 非正文故事内容；这些都会被明确标记为未覆盖。真实大型、复杂、公开招标文件语料集尚未纳入系统性回归，因此当前只可称为工程门禁通过，不能称为真实业务验证完成。

### 需要您拍板

目前没有立即需要拍板的事项。OCR、复杂表格还原和大型真实语料验证将在后续阶段分别提出成本与优先级方案，不会默认扩大范围。

### 待办登记

见文末统一待办清单。

## 统一待办清单

| 事项 | 说明 | 优先级 |
|---|---|---|
| 模型无关证据检索 | 已完成；后续 Agent 只能使用受项目和 Coverage 约束的候选结果 | 已完成 |
| Task 8A 模型调用门禁 | 真实业务文本进入模型前，完成调用台账、预算、范围与人工确认验收 | 强制门禁 |
| 明确 `REVIEW_MODEL` | 已确认 `gpt-5.6-luna`；OpenAI 主通道 smoke 仍可按需执行 | 已完成配置 |
| EasyRouter 备用通道 | 已确认 `deepseek-v4-flash` 并完成无业务文本 smoke；自动故障切换仍待独立治理 | 已完成连通性 |
| 复杂文档能力 | 分别评估 OCR、表格几何、附件和 Word 非正文内容提取的价值与成本 | 后续评估 |
| 真实招标文件回归集 | 在来源、授权和脱敏边界明确后，建立复杂公开样本验证集 | 业务验证前 |
| GitHub 协作入口 | 安全衔接远端初始提交并推送功能分支，不覆盖远端 `main` | 本轮收尾 |

## 第四章 · 给模型接电，但先不把标书插上去（09-20，PR 号待补 · Task 8 模型边界收官）

### 这次做了什么（大白话）

- **把模型开关集中管理**：像给整栋楼只留一个总闸，提取模型和复核模型从统一配置进入，Agent 代码不各自私藏供应商和模型名。
- **模型没配好就停在门口**：缺少模型名、供应商不支持或配置用途写错，程序给出稳定错误码；健康检查和文件解析仍然可以工作。
- **Smoke 只做通电测试**：它最多运行一轮、没有工具、只发送固定的 readiness 文案，成功只打印 `OK`，失败不打印 URL、请求头、环境变量或 SDK 异常原文。

### 先说难堪的

第一版代码虽然能跑离线测试，独立验收发现 timeout、重试、单轮、无工具等关键边界没有被测试锁住；补上断言后才通过最终验收。这再次说明“代码里写了限制”不等于“限制真的守得住”。

### 给您的交代

- 最终提交：`ea8ba6d`，边界测试加固提交：`d2da0bb`。
- Task 8 专项测试 **14 项通过**；本地全套测试 **233 项通过**；Ruff、Mypy 和差异检查通过。
- 独立最终验收：**Critical 0、Important 0、Minor 0，Ready = Yes**。
- `Constitution impact: Yes` 已正式记录：本任务新增模型供应商边界，但没有创建 Evidence、Assessment、DisplayStatus，也没有扩大 Agent 工具或业务文档权限。
- 初次 smoke 因未配置模型名安全返回 `ERROR MODEL_NOT_CONFIGURED`，没有发出 API 请求。
- 用户确认 `deepseek-v4-flash` 后，EasyRouter 显式 smoke 输出 **`OK`**；只发送固定 readiness 文案，没有发送业务文本。

当前边界：EasyRouter 通道已连通，但这只证明最小 API 调用可用，不代表模型具备投标审查质量。OpenAI 主模型仍按显式配置使用，两个供应商之间尚未启用自动故障切换。

### 需要您拍板

当前没有新增拍板事项。若以后启用 OpenAI → EasyRouter 自动故障切换，必须单独定义可切换错误、预算、调用账本和人工可见语义，并重新进行 Constitution impact 审查。

### 待办登记

见文末统一待办清单。

## 第五章 · 模型先领门禁卡，才能看业务材料（09-21，PR 号待补 · Task 8A 治理门禁）

### 这次做了什么（大白话）

- **给模型准备一张“只在本项目有效”的门禁卡**：`ReviewContext` 由服务端写入项目、招标版本、企业资料版本、可见片段和预算；模型不能靠自己传一个新的项目编号或版本编号来扩大范围。
- **把“看了哪些、没看哪些”记成正式 Coverage**：完整、部分失败、待人工确认三种状态不能混写。没有检查的范围必须写出来，像盘点仓库时明确哪些货架还没开箱。
- **建立调用账本**：每次模型尝试保存供应商、模型、Prompt 版本与哈希、输入对象编号、覆盖范围、结果、失败码、耗时、重试、用量和停止原因；默认不保存整段 Prompt、整份文件或密钥。
- **企业资料必须被项目明确选入**：不是“系统里存在”就能给模型看；选入关系会记录操作者和时间，并在生成上下文时再次验明确实属于企业资料。
- **招标补遗和澄清不能靠模型猜先后**：招标包记录纳入/排除、效力顺序和冲突；冲突未解决时直接停在人工确认。
- **预算先验算，再允许调用**：轮次、工具次数、重试、超时、token、费用和批量都有服务端上限，耗尽后停止，不会自动放宽。

### 先说难堪的

这一步仍然没有把真实招标文件交给模型。我们先把“模型什么时候有资格看材料”做成程序门禁，再谈要求提取；否则就像还没装门锁就把档案室钥匙交出去。当前测试是程序化治理证明，不是模型审核质量证明。

### 给您的交代

- 新增治理对象：`Coverage`、`ReviewContext`、`LLMCallRecord`、`ProjectCompanyEvidence`、`TenderPackageMember` 和运行预算。
- Task 8A 专项测试 **11 项通过**；全量测试 **244 项通过**；新增范围 Ruff、Mypy 通过。
- 独立攻击式复审最终结论：Critical 0、Important 0、Minor 0，`Ready = Yes`。
- 真实 `backend/bidguard.db` 未被测试触碰，原有大小、哈希和修改时间保持不变。
- `Constitution impact: Yes` 已单独记录在 `docs/governance/task-8a-constitution-impact.md`。

### 当前边界

Task 8A 尚未创建正式 Requirement、EvidenceLink、Assessment 或 DisplayStatus，也尚未实现要求提取 Agent。下一步只能在独立复审通过后，进行单文件、有界、可回放的 Task 9 提取评测；不能把它宣传成完整招标包审核。

### 需要您拍板

目前没有新增拍板事项。若 Task 8A 独立复审通过，我会先提交治理基础，再进入 Task 9 的“引用门禁 + 受限要求提取”开发。

### 待办登记

见文末统一待办清单。
